import 'package:flutter/material.dart';

class StreakScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Streak Character'),
      ),
      body: Center(
        child: Text('Streak Character Screen'),
      ),
    );
  }
}
