import 'package:flutter/material.dart';

class InsightsSummary extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Insights and Summaries will be here'),
    );
  }
}
